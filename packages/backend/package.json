{
  "name": "@budget/backend",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "scripts": {
    "dev": "convex dev",
    "dev:setup": "convex dev --configure --until-success",
    "check-types": "npx tsgo --noEmit",
    "deploy": "convex deploy",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "convex": "catalog:",
    "dotenv": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@budget/config": "workspace:*",
    "@types/node": "^24.3.0",
    "typescript": "catalog:",
    "vitest": "^3.2.3"
  }
}
