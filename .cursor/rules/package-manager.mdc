---
description: Use bun as the package manager for all commands
alwaysApply: true
---

# Package Manager

This project uses **bun**. Always use `bunx` instead of `npx` when running package executables.

- ✅ `bunx shadcn@latest add button`
- ❌ `npx shadcn@latest add button`
